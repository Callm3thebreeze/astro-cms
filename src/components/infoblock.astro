---
import { Picture } from "astro:assets";
import { Icon } from "astro-icon/components";
import Link from "@components/ui/link.astro";

const { content } = Astro.props;
---

<div class="grid lg:grid-cols-2 place-items-center mt-16 md:mt-20 gap-5">
  <div>
    {content.title && <h1 class={content.titleClass}>{content.title}</h1>}
    {
      content.content && (
        <p class="text-lg mt-4 text-slate-600 max-w-xl">{content.content}</p>
      )
    }
    {
      content.links && content.links.length > 0 && (
        <div class="mt-6 flex flex-col sm:flex-row gap-3">
          {content.links.map((link: any) => (
            <Link
              href={link.href}
              size="lg"
              style={link.style}
              rel="noopener"
              class="flex gap-1 items-center justify-center"
              target="_blank">
              {link.icon.name && (
                <Icon class={link.icon.class} name={link.icon.name} />
              )}
              {link.text}
            </Link>
          ))}
        </div>
      )
    }
  </div>
  {
    content.image.src && (
      <div class={content.image.class}>
        <Picture
          class={content.image.picClass}
          src={content.image.src}
          alt={content.image.alt}
          width="250"
          height="300"
          loading="eager"
          format="avif"
        />
      </div>
    )
  }
</div>
