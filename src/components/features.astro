---
import { Icon } from "astro-icon/components";
import { features } from "@content/features.js";
---

{
  features && (
    <div class="mt-16 md:mt-20">
      {features.title && (
        <h2 class="text-4xl lg:text-5xl font-bold lg:tracking-tight">
          {features.title}
        </h2>
      )}
      {features.subtitle && (
        <p class="text-lg mt-4 text-slate-600">{features.subtitle}</p>
      )}
    </div>
  )
}

{
  features && features.items && features.items.length > 0 && (
    <div class="grid sm:grid-cols-2 md:grid-cols-3 mt-16 gap-16">
      {features.items.map((item, index) => (
        <div key={index} class="flex gap-4 items-start">
          {item.icon && (
            <div class="mt-1 bg-black rounded-full p-2 w-8 h-8 shrink-0">
              <Icon class="text-white" name={item.icon} />
            </div>
          )}
          <div>
            {item.title && <h3 class="font-semibold text-lg">{item.title}</h3>}
            {item.description && (
              <p class="text-slate-500 mt-2 leading-relaxed">
                {item.description}
              </p>
            )}
          </div>
        </div>
      ))}
    </div>
  )
}
